import { FilterFormatLPipe } from './filter-format-l.pipe';

describe('FilterFormatLPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFormatLPipe();
    expect(pipe).toBeTruthy();
  });
});
